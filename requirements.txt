# OzzyVision Lab - Python Dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
websockets>=12.0
# FastAPI'nin UploadFile / Form uç noktaları için ZORUNLU (asset ve LoRA yükleme)
python-multipart>=0.0.9
pillow>=10.2.0
httpx>=0.27.0
requests>=2.31.0
aiohttp>=3.9.0
anyio>=4.3.0
# LoRA ve model indirmeleri
huggingface_hub>=0.23.0
# Video çıktısını tarayıcıda oynatılabilir biçime dönüştürme (ComfyUI de aynı sürümü kullanır)
av>=17.0.0
pytest>=8.0.0

# NOT: torch (CUDA sürümü) ortama göre ayrıca kurulmalıdır.
# Colab'da hazır gelir. Yerel kurulum için:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# torch bulunamazsa API çalışmaya devam eder, yalnızca GPU raporlaması devre dışı kalır.
